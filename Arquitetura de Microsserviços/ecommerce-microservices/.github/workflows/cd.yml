name: CD

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Kubernetes (placeholder)
        run: |
          echo "Configure kubectl and run: kubectl apply -k infrastructure/kubernetes/overlays/production"
          echo "Or use Helm/ArgoCD. No actual deploy in this placeholder."
