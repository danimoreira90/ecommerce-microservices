# Copy to secrets.yaml and fill with real values. NEVER commit secrets.yaml
# kubectl create secret generic ecommerce-secrets --from-literal=db.password=xxx ...
apiVersion: v1
kind: Secret
metadata:
  name: ecommerce-secrets
  namespace: ecommerce
type: Opaque
stringData:
  db.password: "REPLACE_ME"
  jwt.secret: "REPLACE_ME"
  stripe.secret_key: "REPLACE_ME"
  sendgrid.api_key: "REPLACE_ME"
